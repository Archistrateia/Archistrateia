name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
        
    - name: Restore dependencies
      run: dotnet restore

    - name: Lint (Analyzers)
      run: dotnet build --no-restore -warnaserror
      
    - name: Build
      run: dotnet build --no-restore
      
    - name: Test
      run: dotnet test --no-build --verbosity normal

  sonar:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: test

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Install SonarScanner for .NET
      run: dotnet tool update --global dotnet-sonarscanner

    - name: Install dotnet-coverage
      run: dotnet tool update --global dotnet-coverage

    - name: Generate Coverage (NUnit phase)
      run: |
        if [ -x "/Applications/Godot_mono.app/Contents/MacOS/Godot" ] || command -v godot >/dev/null 2>&1; then
          ./run_tests.sh nunit --show-failures-only --coverage --coverage-output TestResults/coverage/coverage.xml
        else
          echo "Godot executable not found on runner; skipping coverage generation."
        fi

    - name: Sonar Analyze
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      run: |
        dotnet sonarscanner begin /k:"Archistrateia_Archistrateia" /o:"archistrateia" /d:sonar.token="${SONAR_TOKEN}" /d:sonar.cs.vscoveragexml.reportsPaths="TestResults/coverage/coverage.xml"
        dotnet build
        dotnet sonarscanner end /d:sonar.token="${SONAR_TOKEN}"
